Work on the supplied HamaraCommerce(5) repaired source. Read REPAIR_NOTES.md first. Actually run the commands/checks below; do not just report a plan. Preserve the current UI.

1. Locate the existing repository and the extracted updated source. Back up the current project and database. Compare the source before copying it into the existing checkout. Preserve its .git history, current connection strings, secrets, environment settings and any newer uploads. Do not copy bin/obj/.vs back and do not replace/drop the database. Do not restore old controllers/services over the supplied repairs.

2. Check .NET 9 SDK and SQL Server/LocalDB availability. Run dotnet restore HamaraCommerce.sln, dotnet build HamaraCommerce.sln, and dotnet test Tests/HamaraCommerce.Tests/HamaraCommerce.Tests.csproj --logger trx. Tests must use disposable test databases, never the store database. Optional HAMARA_TEST_SQL_CONNECTION configures a SQL server; the test factory selects separate HamaraCommerce_* database names. No InMemory fallback for SQL concurrency tests. Fix real build/test failures without weakening assertions or reverting reliability protections.

3. Use EF tooling compatible with EF Core 9.0.2. The new migration is ALREADY supplied: 20260905223000_RepairCheckoutOutboxAndLifecycle. Do not generate a duplicate. Run dotnet ef migrations has-pending-model-changes and inspect the migration SQL. Resolve any genuine model/snapshot discrepancy. Apply the migration first to a disposable copy of the current database, verify existing data survives, then run dotnet ef database update against the backed-up intended database. If duplicate coupon redemptions block the unique index, report and reconcile the real records; never delete records blindly or bypass the constraint.

4. Run the app locally. Verify guest and logged-in checkout; replay after cart clearing; concurrent recovery; no second gateway call after an uncertain result; cancellation stock/coupon rollback; return request remains awaiting review; old sessions rejected after password reset/deactivation. Run the outbox tests for two workers, crash recovery and blocked-to-working SMTP. Configure actual SMTP locally when available; otherwise report real delivery BLOCKED. For Production set PublicSiteUrl to the actual HTTPS store URL; do not invent a domain or publish localhost links. Never reset an unresolved payment record to force a charge.

5. Browser-check Home, Shop, Details, Cart, Checkout, Login, Account and Admin in both themes, desktop/mobile. Compare the existing appearance, check console errors and verify all actions. Correct regressions without redesigning the UI.

6. After build/tests/migrations/browser checks succeed, review git diff, exclude credentials and generated artifacts, commit the source fixes, and push normally to the current remote branch of https://github.com/abdullahazaam/Hamara-Ecommerce.git. Do not force-push. Report commit/branch, actual command results, migration result and screenshots. If a required check is blocked, state the blocker and do not claim completion or push an unverified release.
